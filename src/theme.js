import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#6200EE',
    },
    secondary: {
      main: '#03DAC6',
    },
    background: {
      default: '#121212',
    },
    text: {
      primary: '#FFFFFF',
      secondary: '#BB86FC',
    },
  },
});

export default theme;
