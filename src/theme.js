import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  typography: {
    fontSize: 11
  },
  palette: {
    primary: {
      main: '#000000',
    },
    secondary: {
      main: '#FFFFFF',
    },
  },
});

export default theme;
